# 演示变量作用域：兄弟 if 块之间不能共享局部变量
# 正确做法是将需要在多个块间共享的变量声明在外层作用域
def main(args:list[str]) {
    flag_a: int = 1
    v: int = 0           # 在外层作用域声明
    if flag_a == 1 {
        v = 42           # 在 if 块内赋值（不是声明）
    }
    if flag_a == 1 {
        print(v, "\n")   # 这里可以访问外层的 v
    }
}
